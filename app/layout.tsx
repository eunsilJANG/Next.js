import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "NextPortfolio - Modern Portfolio Built with Next.js",
  description: "A modern portfolio website built with Next.js 15, React 19, and Tailwind CSS. Showcasing the latest web technologies and best practices.",
  keywords: ["Next.js", "React", "Tailwind CSS", "Portfolio", "Web Development"],
  authors: [{ name: "NextPortfolio" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${poppins.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
